cmake_minimum_required(VERSION 2.8)
project( ASCII-IMAGE-convert )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( ASCII-IMAGE-convert main.cpp )
target_link_libraries( ASCII-IMAGE-convert ${OpenCV_LIBS} )
